---
// Footer component with attribution
---

<footer class="footer">
  <div class="footer-content">
    <div class="footer-message">
      <p class="footer-text">
        This tool was AI-coded with Claude to solve a recurring problem I faced as an app owner: spending hours manually resizing screenshots for App Store submissions. Now it takes seconds.
      </p>
    </div>
    <div class="footer-attribution">
      <p class="footer-credit">
        Built by <a
          href="https://dennislysenko.tech"
          target="_blank"
          rel="noopener noreferrer"
          class="footer-link"
        >
          Dennis Lysenko
        </a>
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    margin-top: clamp(5rem, 10vw, 8rem);
    padding-top: clamp(3rem, 6vw, 4rem);
    padding-bottom: clamp(2rem, 4vw, 3rem);
    border-top: 1px solid var(--color-gray-2);
  }

  .footer-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 4vw, 3rem);
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .footer-message {
    max-width: 700px;
  }

  .footer-text {
    font-size: 1rem;
    line-height: 1.65;
    color: var(--color-text-secondary);
    font-weight: 400;
  }

  .footer-attribution {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .footer-credit {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    font-weight: 400;
  }

  .footer-link {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
    position: relative;
    transition: color 0.2s ease;
  }

  .footer-link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 100%;
    height: 1px;
    background: currentColor;
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .footer-link:hover {
    color: var(--color-blue);
  }

  .footer-link:hover::after {
    opacity: 1;
  }

  @media (min-width: 768px) {
    .footer-content {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }

    .footer-attribution {
      flex-shrink: 0;
      padding-top: 0.125rem;
    }
  }

  @media (min-width: 1024px) {
    .footer-message {
      max-width: 800px;
    }
  }
</style>
